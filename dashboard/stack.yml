provider:
  name: faas
  gateway: http://127.0.0.1:8080

functions:
  system-dashboard:
    lang: node10-express
    handler: ./of-cloud-dashboard
    image: functions/of-cloud-dashboard:0.6.2
    labels:
      openfaas-cloud: "1"
      role: openfaas-system
    environment_file:
      - dashboard_config.yml
